import React from 'react'

import { ScrollView } from 'react-native'

export default class InstancesList extends React.Component {
  render () {
    return (
      <ScrollView>
        <Text>Instances List</Text>
        {
          
        }
      </ScrollView>
    )
  }
}
